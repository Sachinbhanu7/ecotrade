{
  "name": "ecotrade-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build-frontend && npm run build-admin && npm run merge",
    "build-frontend": "cd frontend && npm install && npm run build",
    "build-admin": "cd admin && npm install && npm run build",
    "merge": "mkdir -p dist && cp -r frontend/dist/* dist/ && mkdir -p dist/admin-panel && cp -r admin/dist/* dist/admin-panel/",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:admin\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:backend": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
